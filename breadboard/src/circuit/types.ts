export enum NetState {
  FLOAT = "FLOAT",
  LOW = "LOW",
  HIGH = "HIGH",
  CONFLICT = "CONFLICT",
}

export enum PinRole {
  INPUT = "INPUT",
  OUTPUT = "OUTPUT",
  BIDIRECTIONAL = "BIDIRECTIONAL",
  POWER = "POWER",
}

export enum EdgeKind {
  RISING = "RISING",
  FALLING = "FALLING",
}
